---
import { db, Level, count } from 'astro:db';
import Sokoban from "../components/Sokoban.vue";
import Layout from "../layouts/Layout.astro";
import type { Level as ILevel } from "../types";

const data = await db.select({ count: count() }).from(Level);
console.log(data);

const level: ILevel = {
  id: 522,
  height: 7,
  width: 6,
  map: "111177104177100111152001100301100111111177",
  moves: 33,
  pushes: 8,
};
---

<Layout>
  <Sokoban level={level} client:load />
</Layout>
